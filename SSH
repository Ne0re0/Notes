SSH

to login :
	$ ssh username@ip
	or :
	chmod 600 id_rsa
	ssh -i <private id_rsa file> <username>@<ip>

to bruteforce password :
	hydra -t 16 -l USERNAME -P /usr/share/wordlists/rockyou.txt -vV 10.10.230.250 ssh

options : 
		chmod 600 id_rsa	-> changer les permissions pour eviter les erreurs ? 
		-i  id_rsa		-> clé de connection
		
ERROR :
	If you get an error saying Unable to negotiate with <IP> port 22: no matching how to key 
	type found. Their offer: ssh-rsa, ssh-dss this is because OpenSSH have deprecated ssh-rsa.
	Add 
	
	-oHostKeyAlgorithms=+ssh-rsa to your command to connect.
		
commande in SSH :

└─$ wget -i downloads.txt
└─$ scp mission24@10.10.218.73:/tmp/bribe .

PORT FORWARDING
En remote :
└─$ ss -tunlp -> on regarde les ports peu communs

En local :
	  localPort      remotePort  remoteusername@remoteIP
└─$ ssh -L 10000:localhost:10000 <username>@<ip>
Dans firefox : http://localhost:10000
